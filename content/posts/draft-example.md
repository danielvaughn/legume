---
title: A draft in progress
summary: This post demonstrates draft status.
draft: true
---

This post is visible while running `npm run dev` but excluded from
production builds. Flip `draft: true` to `false` (or remove the line) to
publish it.

Note that the résumé may not reference a draft post — the production build
fails until the post is published or the reference is removed.
